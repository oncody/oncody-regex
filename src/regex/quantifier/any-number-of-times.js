
const ANY_NUMBER_OF_TIMES = '*';

export default class ANY_NUMBER_OF_TIMES extends RegexQuantifier {
    constructor() {
        super();
    }
}
static AT_LEAST_ONCE = new RegexQuantifier('+');
static NEVER_OR_ONCE = new RegexQuantifier('?');